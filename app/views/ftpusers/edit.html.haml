%h1 Editing ftpuser

= error_messages_for :ftpuser

- form_for(:ftpuser, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Update"

= link_to 'Show', object_path
|
= link_to 'Back', objects_path
